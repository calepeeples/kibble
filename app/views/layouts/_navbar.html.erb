<%# navbar styled for Bootstrap 3.0 %>
	 <div class="navbar navbar-grnds navbar-fixed-top">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
					<span class="navbar-brand"><%= link_to(image_tag("grlogo-white.png"), { :controller => "home", :action => "index"}) %></span>
        </div>
        <div class="navbar-collapse collapse">
					<ul class="nav navbar-nav navbar-right">
						<% if false %>
						<% if current_user.jamba == true %><li><%= image_tag "logo-jambajuice.png", :class => "cobrand" %></li><% else %> hi <% end %>
						<% end %>
				      <li class="dropdown active">
								<% if signed_in? %>
					      <li class="dropdown active">
					        <%= link_to current_user.firstname + " " + current_user.lastname, "#", :class =>"dropdown-toggle", "data-toggle" => "dropdown" %>
					        <ul class="dropdown-menu">
										<li><%= link_to 'Homepage', current_user %></li>
										<li class="divider"></li>
										<li><a href="#">Case: Deep Vein Trombones</a></li>
										<li><a href="#">Case: Acute Rectal-Crainial Translocation</a></li>
										<li class="divider"></li>
					          <li><a href="#">Start a New Visit</a></li>
					          <li><a href="#">Start a New Opinion</a></li>
					          <li class="divider"></li>
										<li><%= link_to 'Account Settings', edit_user_path(current_user) %></li>
					          <li><%= link_to "Sign out", signout_path, method: "delete" %></li>
					        </ul>
					      </li>
			          <% else %>
			            <%= link_to "Sign in", signin_path, :class => "dropdown-toggle" %>
			          <% end %>
				      </li>
				    </ul>
        </div><!--/.navbar-collapse -->
      </div>
    </div>