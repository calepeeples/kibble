<% content_for :title do %>Grand Rounds<% end %>
<% content_for :description do %>Grand Rounds<% end %>
	<div class="container">
	<h1>Listing users</h1>
<ul>
	<% @users.each do |user| %>
	    <li><%= user.firstname %> <%= user.email %> <%= link_to 'Show', user %> <%= link_to 'Edit', edit_user_path(user) %> <%= link_to 'Destroy', user, method: :delete,
	                                     data: { confirm: 'Are you sure?' } %>
	  
	<% end %>
</ul>
	<br />

	<%= link_to 'New User', new_user_path %>
	</div>
